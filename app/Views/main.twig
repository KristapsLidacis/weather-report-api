<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>News Page</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
          integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <style>
        .container {
            max-width: 45%;
            color: antiquewhite;
            opacity: 0.9;
        }

        body {
            background-color: #FFC300;
        }

        .description {
            font-size: 1.5vw;
        }

        .feedTitle {
            padding-top: 25px;
        }
        a, a:hover{
            text-decoration: none;
            color: inherit;
        }
        .container:hover {

            opacity: 1;
        }

        .headliner {
            color: antiquewhite;
            margin: auto;
            text-align: center;
            padding-bottom: 0.5em;
        }

        .feedContainer {
            padding-top: 0.8em;
        }
    </style>
</head>
<body>

<div class="container rounded bg-dark">
    <h1 class="headliner">Top US Tech news feed</h1>
</div>
<div class="feedContainer">
    {% for news in newsList %}
        <div class="container rounded bg-dark">
            <a href="{{ news.getUrl }}" target="_blank">
                <figure class="text-center feedTitle">
                    <blockquote class="blockquote">
                        <h3 class="h3">{{ news.getTitle }}</h3>
                    </blockquote>
                    <img src="{{ news.getImageUrl }}" class="img-thumbnail img-fluid center">
                    <p class="description">{{ news.getDescription }}</p>
                    <figcaption class="blockquote-footer">
                        <cite title="Source Title">{{ news.getSource }}</cite>
                    </figcaption>
                </figure>
            </a>
        </div>
    {% endfor %}
</div>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
        crossorigin="anonymous"></script>
</body>
</html>